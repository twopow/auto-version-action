name: Auto Version
description: 'Automatically calculate version'

outputs:
  version:
    description: generated version
  shortlog:
    description: git shortlog

runs:
  using: 'node20'
  main: 'index.js'
